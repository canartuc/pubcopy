# Code Fence Protection

Outside math: $a+b$ should render.

```bash
echo $HOME costs $5
grep "==pattern==" file.txt
- [ ] not a task
> [!note] not a callout
![[not-an-embed.png]]
```

Inline `$x$ and ==y==` code span.

Real ==highlight== outside code.
