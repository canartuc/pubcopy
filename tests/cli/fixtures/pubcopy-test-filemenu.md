# FileMenu Target QXZ123

This note is copied via the file-menu code path while a DIFFERENT note is active.

- It must reach the clipboard even though it is not open.
- Marker: FILEMENU-CONTENT-MARKER
